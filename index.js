import ReactDOM from "react-dom/client";
import Assign from "./assignment";
const rootElement = document.getElementById('root');
const rootRef = ReactDOM.createRoot(rootElement);
rootRef.render(<Assign/>); 
